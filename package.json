{
  "name": "kaisapaisa",
  "version": "1.0.0",
  "private": true,
  "description": "KAISAPAISA - Personal Finance Management Web Application",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run build --workspace=frontend && node backend/server.js",
    "build": "npm run build --workspace=frontend",
    "start": "node backend/server.js"
  }
}
